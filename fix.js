const fs = require('fs'); let content = fs.readFileSync('qinshihuang/js/cyx_bookingHandler.js', 'utf8'); content = content.replace('if (ttMins < 360) ttMins += 1440; // past midnight logic', 'const openHour = window.SYSTEM_CONFIG?.OPERATION_TIME?.OPEN_HOUR || 8; if (ttMins < openHour * 60) ttMins += 1440; // past midnight logic'); fs.writeFileSync('qinshihuang/js/cyx_bookingHandler.js', content);
